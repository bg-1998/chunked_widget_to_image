# Run with `dart run ffigen --config ffigen.yaml`.
name: ChunkedWidgetToImageBindings
description: |
  Bindings for `src/chunked_widget_to_image.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/chunked_widget_to_image_bindings_generated.dart'
headers:
  entry-points:
    - 'src/chunked_widget_to_image.h'
  include-directives:
    - 'src/chunked_widget_to_image.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
